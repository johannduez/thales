import { Cookieauthentification } from './cookieauthentification';

describe('Cookieauthentification', () => {
  it('should create an instance', () => {
    expect(new Cookieauthentification()).toBeTruthy();
  });
});
